[project]
name = "modelable-showcase-tests"
version = "0.0.0"
description = "Test/tooling dependencies for the Modelable showcase (tests/, scripts/). Not a distributable package."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "clickhouse-connect>=0.8",
  "fastavro>=1.9",
  "jsonschema>=4.20",
  "openapi-spec-validator>=0.7",
  "psycopg[binary]>=3.2",
  "pytest>=8.0",
  "pyyaml>=6.0",
]

[tool.uv]
package = false
